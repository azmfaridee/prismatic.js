var Drawable = new JS.Class({
    initialize: function(arg) {
    	// drawable object base class
    	// others will extend this class
    },

    draw: function(arg) {
    	// base draw function
    }
});
